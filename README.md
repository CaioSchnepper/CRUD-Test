Projeto Spring Boot pode subir pelo comando mvn spring-boot:run.

Projeto Angular pode subir pelo comando ng serve --port 8081 (Os endpoints da API são configurados para receber requisições da porta 8081).
